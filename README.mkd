# Puppet Network Configuration module.

A simple hiera wrapper for the `network_config` and `network_route` resources provided by the puppet-network plugin (https://github.com/adrienthebo/puppet-network).
